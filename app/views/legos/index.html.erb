<div class="container mt-3">
  <div class="row">
    <div class="col-12">
      <h1>List of Legos</h1>
    </div>
  </div>

  <div>
    <%= form_with url: legos_path, method: :get, class: "d-flex" do %>
      <%= text_field_tag :query,
        params[:query],
        class: "form-control",
        placeholder: "Find a lego"
      %>
      <%= submit_tag "Search", class: "btn btn-primary" %>
    <% end %>
  </div>

  <div class="row">
    <div class="col">
      <% if @query %> <p>You searched for <strong><%= @query %></strong>. We found <%= @legos.count %> legos out of our <%= Lego.count %> on our website.</p> <% else %>
        <p>All <%= Lego.count %> legos.</p> <% end %>
    </div>
  </div>

  <div class="row">
    <% @legos.each do |lego| %>
      <div class="col-md-4">
        <div class="lego-card">
            <% if lego.photo.attached? %> <%= cl_image_tag lego.photo.key, height: 300, crop: :fill, class:"w-100"%><% end %>
          <div class="lego-card-content">
            <div class="lego-card-title">
            <div class="flex">
              <h4><%= lego.title %></h4></div>
            </div>
            <p><%= lego.location %></p>
              <div class="flex align-items-center">
                <span><%= lego.description.truncate(40) %></span>
              </div>
              <div class="flex">
                <div class="flex justify-content-around">
                  <div class="flex align-items-center ms-3"> <span><h3><b><%= lego.price %></b></span>  <i class="fa-solid fa-euro-sign"></i></h3></div>
                </div>
              </div>
            </div>
          <%= link_to '', lego_path(lego), class: 'card-link'  %>
        </div>
      </div>
    <% end %>
  </div>
</div>

<div style="height: 10px">
  </div>
            <%# <%= link_to "Edit", edit_lego_path(lego), class: "ms-3 text-warning" %>
            <%# <%= link_to "Remove", lego_path(lego), %>
                <%# data: { turbo_method: :delete, turbo_confirm: "Are you sure?" },
                class: "ms-3 text-danger %>

            <%# link_to "Edit", edit_lego_path(lego), class: "ms-3 text-warning" %>
            <%# link_to "Remove", lego_path(lego),
                data: { turbo_method: :delete, turbo_confirm: "Are you sure?" },
                class: "ms-3 text-danger"
            %>
